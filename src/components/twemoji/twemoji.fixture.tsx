import { Text } from "natmfat";
import { Twemoji } from ".";
import { Wrapper } from "../../wrapper";

export default (
  <Wrapper className="max-w-md">
    <Text multiline>
      Building apps that shine <Twemoji emoji="✨" /> with React and Tailwind.
      Currently using Typescript, but attempting to learn Go. In my spare time I
      do generative art, make origami dragons, or vibe to Lo-Fi.
    </Text>
  </Wrapper>
);
